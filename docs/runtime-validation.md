# Runtime validation

Scheduled and manual attendance now perform a focused credential/configuration preflight and install production dependencies only. The complete unit, security, client metadata, YoStar, and protocol validation suite remains on pull requests and relevant pushes.
