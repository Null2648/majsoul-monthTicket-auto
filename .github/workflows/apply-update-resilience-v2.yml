name: Apply Update Resilience Fix V2

on:
  push:
    branches:
      - agent/repair-update-resilience
    paths:
      - '.github/workflows/apply-update-resilience-v2.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repair branch
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: agent/repair-update-resilience
          fetch-depth: 0

      - name: Apply compatibility patch
        run: python3 scripts/apply-update-resilience-fix.py

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run regression suite
        run: npm test

      - name: Remove temporary repair helpers
        shell: bash
        run: |
          rm -f scripts/update-probe.js
          rm -f scripts/apply-update-resilience-fix.py
          rm -f .github/workflows/inspect-latest-attendance.yml
          rm -f .github/workflows/apply-update-resilience-fix.yml
          rm -f .github/workflows/apply-update-resilience-v2.yml

      - name: Commit repair
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Repair automatic client-version recovery'
          git pull --rebase origin agent/repair-update-resilience
          git push origin HEAD:agent/repair-update-resilience
